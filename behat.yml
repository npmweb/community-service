default:
    autoload:
        '': %paths.base%/app/tests/acceptance/bootstrap
    suites:
        default:
            paths:
                - %paths.base%/app/tests/acceptance
    extensions:
        Behat\MinkExtension:
            base_url: http://local.communityservice.org
            sessions:
                default:
                    selenium2:
                      wd_host: http://localhost:8643/wd/hub
